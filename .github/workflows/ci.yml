name: ci

on:
  pull_request:
  push:
    branches: [master]

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: ${{ github.ref != 'refs/heads/master' }}

jobs:
  test:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 26
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - run: pnpm test

  e2e:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 26
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - uses: actions/cache@v4
        id: pw-cache
        with:
          path: ~/.cache/ms-playwright
          key: pw-${{ runner.os }}-${{ hashFiles('pnpm-lock.yaml') }}
      - if: steps.pw-cache.outputs.cache-hit != 'true'
        run: pnpm exec playwright install --with-deps chromium
      - if: steps.pw-cache.outputs.cache-hit == 'true'
        run: pnpm exec playwright install-deps chromium
      - run: pnpm build
      - name: shipped build carries no component gallery
        # The gallery is a design surface, opt-in at build time. What a plain
        # build must never emit: its lazy chunk, or the nav label only it has.
        run: |
          if ls dist/assets/GalleryScreen-*.js >/dev/null 2>&1; then
            echo "::error::the build ships the gallery chunk"
            exit 1
          fi
          if grep -rqF 'price · dots' dist; then
            echo "::error::the build ships gallery markup"
            exit 1
          fi
          echo "gallery is out of the shipped build"
      # Rebuilds dist with the gallery in it: the visual and a11y specs open it.
      - run: pnpm e2e
      - name: the gallery check above can still fail
        # Both markers are searched for, not merely absent, in a build that is
        # meant to carry them. Renaming either one would otherwise leave the
        # check above passing over a build it can no longer recognise.
        run: |
          if ! ls dist/assets/GalleryScreen-*.js >/dev/null 2>&1; then
            echo "::error::a gallery build emits no GalleryScreen chunk — the check above proves nothing"
            exit 1
          fi
          if ! grep -rqF 'price · dots' dist; then
            echo "::error::the gallery marker changed — update the check above to match"
            exit 1
          fi
          echo "both markers present in a gallery build, so their absence means something"

  lint:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 26
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - run: pnpm exec biome ci .
      - run: pnpm typecheck

  version-bump:
    if: github.event_name == 'pull_request'
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: require version increase vs base
        run: |
          base=$( (git show '${{ github.event.pull_request.base.sha }}:package.json' 2>/dev/null || true) | jq -r '.version // empty')
          head=$(jq -r .version package.json)
          if [ -z "$base" ]; then
            echo "no package.json on base - first version $head accepted"
            exit 0
          fi
          if [ "$base" != "$head" ] && [ "$(printf '%s\n%s\n' "$base" "$head" | sort -V | tail -1)" = "$head" ]; then
            echo "version bump ok: $base -> $head"
          else
            echo "::error::package.json version ($head) must increase vs base ($base)"
            exit 1
          fi

  lock-check:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 26
          cache: pnpm
      - run: pnpm install --lockfile-only
      - run: git diff --exit-code pnpm-lock.yaml

  docs-guard:
    if: github.event_name == 'pull_request'
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: warn when code changed without docs
        run: |
          changed=$(git diff --name-only '${{ github.event.pull_request.base.sha }}...HEAD')
          if echo "$changed" | grep -qE '^(src/|deploy/|\.github/)' && ! echo "$changed" | grep -q '^docs/'; then
            echo "::warning::code or infra changed without a docs/ update - confirm the docs are still accurate"
          else
            echo "docs guard ok"
          fi
